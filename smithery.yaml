startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
    additionalProperties: false
  commandFunction: |-
    (config) => ({
      command: "node",
      args: ["./packages/mcp/bin/osv-ui-mcp.js"]
    })
  exampleConfig: {}
